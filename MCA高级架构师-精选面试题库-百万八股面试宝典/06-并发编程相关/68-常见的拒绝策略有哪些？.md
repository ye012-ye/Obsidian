线程池自己提供了4种拒绝策略，供咱们使用，如果这4个不够，也可以自己去实现

拒绝策略提供了一个接口规范，让咱们去实现。

![](../assets/6544ac666c060375.png)

下面是线程池自带的4个：

```plain
AbortPolicy：扔异常。
DiscardPolicy：任务直接丢弃。
CallerRunsPolicy：谁投递的任务，谁自己处理。
DiscardOldestPolicy：将队列中排在最前面的任务干掉，尝试将自己再次投递到线程池。
```

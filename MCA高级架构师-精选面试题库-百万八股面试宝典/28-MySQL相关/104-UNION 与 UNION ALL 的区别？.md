在 SQL 中，`UNION` 和 `UNION ALL` 都用于合并多个 `SELECT` 结果，但行为和性能有明显差异：

1. **去重行为**

- `UNION` 会去除重复行；
- `UNION ALL` 不去重，保留所有行。

2. **性能差异**

- `UNION` 需执行排序或哈希处理去重，开销较大；
- `UNION ALL` 直接拼接结果，执行更快、更省资源。

3. **适用场景**

- 需要唯一结果时用 `UNION`；
- 若允许重复或追求性能，用 `UNION ALL`。

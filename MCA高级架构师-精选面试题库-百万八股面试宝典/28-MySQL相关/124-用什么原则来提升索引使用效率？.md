- **全值匹配**：联合索引查询条件应包含所有列；
- **最左前缀**：索引列按顺序连续；
- **避免在索引列上用函数/类型转换**；
- **范围查询后面的列将失效**；
- **使用覆盖索引**避免 `SELECT *`；
- **LIKE**：左前缀 `%` 模式会失效，应写 `LIKE 'abc%'`；
- **避免** `!=`**、**`IS NULL`**、**`OR`**、**`NOT IN` 等，这些通常不会走索引。

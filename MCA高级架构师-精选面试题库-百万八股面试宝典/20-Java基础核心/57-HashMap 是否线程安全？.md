`HashMap`**不是线程安全的**。其 `get()` 和 `put()` 方法在并发执行时 **不做同步控制**，可能引起以下严重问题：

- 一个线程调用 `put()`，另一个线程可能读取到过期值甚至丢失数据时返回 `null`。
- 在 `put()` 导致重哈希（rehash）或扩容时，其他线程可能访问到不一致的数组状态，出现异常行为或死循环 。
- 即使两个线程操作不同 key，也可能因 hash 冲突改变内部结构，仍会导致丢失元素或读写不一致。

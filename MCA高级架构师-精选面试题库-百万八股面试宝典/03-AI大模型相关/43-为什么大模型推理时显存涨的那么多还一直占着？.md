###### **显存占用来源**

1. **模型参数**：

- FP16下，70B模型需约140GB显存（2 bytes/参数 × 70B）。

2. **KV缓存**：

- 存储历史Token的Key/Value，空间复杂度为 ( O(batch \times seq \times d\_{model}) )。
- 示例：处理4K Token时，70B模型KV缓存占用约20GB。

3. **激活值**：

- 中间计算结果暂存显存，尤其深层网络。

###### **优化方法**

- **量化推理**：将模型权重转为INT8（显存减半，精度损失可控）。
- **卸载技术**：将部分显存数据临时转移至CPU内存（速度较慢）。

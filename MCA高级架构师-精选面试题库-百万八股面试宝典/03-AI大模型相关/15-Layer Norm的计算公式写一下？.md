Layer Normalization（层归一化）是一种常用的归一化方法，主要用于深度学习模型中。它的计算公式如下：

给定输入向量 ，Layer Norm 的计算步骤如下：

1. **计算均值**：

2. **计算方差**：

3. **归一化**：

其中， 是一个很小的常数，用于避免分母为零的情况。  
4. **缩放和平移**：

其中， 和  是可学习的参数，分别用于缩放和平移归一化后的值。

最终，Layer Norm 的输出为 。

总结一下，Layer Norm 的公式可以表示为：

其中：  
•  是输入的均值  
•  是输入的方差  
•  是一个很小的常数（通常为 ）  
•  和  是可学习的参数

在Linux 系统中，可以使用 grep 命令来查找文件中是否包含某内容：

```plain
grep [-r] [-n] "要查找的内容" 文件名/文件夹
```

如果查询某个文件夹中的所有文件是否包含某内容，可以使用-r表示递归查找文件夹下所有文件。-n显示文件中包含内容的行号。

以上问题答案：

```plain
#递归查找/root/test目录下所有包含zs内容的文件及对应内容的行数
grep -rn "zs" /root/test/*
```

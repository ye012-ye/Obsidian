- 接口定义一组方法。任何类型只要实现这些方法，就自动实现该接口。
- 接口变量接收值或指针类型，实现多态。例如：

```go
type Reader interface { Read(p []byte) (n int, err error) }
func Process(r Reader) { … }
```

- 接口通过动态类型和值的组合实现了运行时多态，并通过 `var i interface{} = &MyType{}` 使用。

- 从 Go 1.18 起支持泛型，声明函数或类型时使用类型参数，如：

```go
func MapKeys[K comparable, V any](m map[K]V) []K {
    keys := make([]K, 0, len(m))
    for k := range m {
        keys = append(keys, k)
    }
    return keys
}
```

适用于 map、slice 操作、工具函数、缓存结构等常见场景。

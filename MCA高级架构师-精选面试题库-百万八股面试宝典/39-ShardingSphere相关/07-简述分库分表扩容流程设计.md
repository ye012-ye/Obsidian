1. 初始规划一个合理的分片规模（例如 32 库 × 32 表），满足当前和短期增长。
2. 建立分片规则：如 `uid % 32 -> 库编号`，`(uid /32) % 32 -> 表编号`。
3. 写入阶段按新规则执行，不改变业务逻辑。
4. 扩容时添加新的数据库节点，DBA 将部分数据迁移至新增实例。
5. 更新分片配置（如调整 modulus 模数、节点拓扑），发布上线。业务路由自动指向新实例，无需改 SQL。
6. 校验迁移数据一致性与访问稳定性，慢慢退役旧节点。

​注意数据迁移期间要避免跨分片查询中断，控制迁移步长，必要时同步加载缓存或延迟切换，并避免暂停整个系统。

**分片键**是决定一行数据落在哪个节点（库+表）的位置字段。选择标准：

1. **查询普遍包含**：该字段在大部分查询条件中出现，可让路由找到目标分片，避免全表扫描。
2. **均匀散列**：其分布应尽可能平衡，避免某个节点成为热点。
3. **稳定不变**：一旦写入后通常不修改，避免迁移复杂。

**评估维度**：

- 使用整数做取模分片（如 user\_id % N）易于实现且均匀。
- 如果按时间 range 切分（如时间区间分表），适合日志、订单按时间归档，但可能产生冷热不均。
- 复杂业务中可能根据多个字段组合分片，需要权衡查询效率与路由复杂度。

- **词法 & 语法解析**：将 SQL 解析为 Token，再生成 AST（抽象语法树），提取关键元素：查询字段、表名、条件、分页等。
- **执行计划优化**：根据解析内容与索引信息，调整过滤条件顺序等优化 query 性能。
- **路由计算**：根据提取的分片键值和策略，计算出具体目标实例与表名。
- **SQL 改写**：将逻辑 SQL 重写为多个物理 SQL（可能针对多个分片执行）。
- **执行物理 SQL**：并发向各目标分片提交具体 SQL。
- **结果合并**：业务层将多个结果集合并（例如排序、分页、聚合）后呈现最终结果。

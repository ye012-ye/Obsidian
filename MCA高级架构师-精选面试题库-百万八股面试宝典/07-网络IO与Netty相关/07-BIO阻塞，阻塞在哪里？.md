**阻塞在操作系统层面**

这些阻塞实际上是发生在操作系统层面的。当Java程序调用 `accept()`、`read()`或 `write()`等I/O操作时，它们会触发操作系统级别的I/O操作。如果I/O操作不能立即完成（如等待网络连接、磁盘I/O等），操作系统会将这些线程置于等待状态，直到I/O操作完成或发生异常。

![](../assets/4dc664e930743e07.png)

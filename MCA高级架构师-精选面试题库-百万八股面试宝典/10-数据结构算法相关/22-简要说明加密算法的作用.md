加密算法通过将明文转换为难以理解的密文，保证数据在传输或存储过程中的**保密性**。这其中，对称加密算法使用相同的密钥进行加密和解密，计算速度快且适合大数据量处理，但其安全性依赖密钥的安全分发与保管；非对称加密则使用一对密钥（公钥与私钥），不会将解密密钥暴露，因此更加易于密钥管理和身份认证，但计算过程更复杂且效率较低。M

在实际应用中，这两种方法经常结合使用：先通过非对称算法安全地交换对称密钥，再用高效的对称算法进行数据加密，如 TLS/SSL 协议采用的方式。S

MapReduce中切分分片的方法为“writeSplits(job, submitJobDir)”，该方法返回maptask个数，说明MapReduce中切片数决定maptask个数。

writeSplit(job,submitJobDir)方法中通过新的API进行切片，代码如下：

|  |
| --- |
| private int writeSplits(org.apache.hadoop.mapreduce.JobContext job,  Path jobSubmitDir) throws IOException,  InterruptedException, ClassNotFoundException {  JobConf jConf = (JobConf)job.getConfiguration();  int maps;  if (jConf.getUseNewMapper()) {  maps = writeNewSplits(job, jobSubmitDir);***//新API进行切片***} else {  maps = writeOldSplits(jConf, jobSubmitDir);  }  return maps; } |

WriteNewMapper()方法内容大体如下：

|  |
| --- |
| private <T extends InputSplit>int writeNewSplits(JobContext job, Path jobSubmitDir) throws IOException,  InterruptedException, ClassNotFoundException {  Configuration conf = job.getConfiguration();    **//获取输入格式化类**InputFormat<?, ?> input =  ReflectionUtils.*newInstance*(job.getInputFormatClass(), conf);    **//进行切片**List<InputSplit> splits = input.getSplits(job);  T[] array = (T[]) splits.toArray(new InputSplit[splits.size()]);   ... ...  return array.length; } |

以上代码中input得到的InputFormat输入格式化类对象默认为TextInputFormat.class，也可以通过“mapreduce.job.inputformat.class”参数在提交MapReduce任务时指定输入格式化类。

TextInputFormat类继承自FileInputFormat抽象类，FileInputFormat抽象类又继承自InputFormat抽象类，所以进行切片split的方法“input.getSplits(job)”最终调用到FileInputFormat.getSplits方法，该方法主要内容如下：

|  |
| --- |
| public List<InputSplit> getSplits(JobContext job) throws IOException {  StopWatch sw = new StopWatch().start();  *//minSize表示切片最小大小，该值默认为1* long minSize = Math.*max*(getFormatMinSplitSize(), *getMinSplitSize*(job));  *//maxSize表示切片最大大小，该值默认为Long.MAX\_VALUE* long maxSize = *getMaxSplitSize*(job);   *// generate splits* List<InputSplit> splits = new ArrayList<InputSplit>();*//存放文件的切片Split* List<FileStatus> files = listStatus(job);*//当前job读取的所有文件信息* boolean ignoreDirs = !*getInputDirRecursive*(job)  && job.getConfiguration().getBoolean(*INPUT\_DIR\_NONRECURSIVE\_IGNORE\_SUBDIRS*, false);  *//循环遍历读取的文件* for (FileStatus file: files) {  if (ignoreDirs && file.isDirectory()) {  continue;  }  Path path = file.getPath();*//获取文件路径* long length = file.getLen();*//获取文件大小长度* if (length != 0) {  *//存放所有Block所在的节点信息* BlockLocation[] blkLocations;  if (file instanceof LocatedFileStatus) {  *//本地文件系统* blkLocations = ((LocatedFileStatus) file).getBlockLocations();  } else {  *//分布式文件系统* FileSystem fs = path.getFileSystem(job.getConfiguration());  *//获取所有block所在节点信息* blkLocations = fs.getFileBlockLocations(file, 0, length);  }  if (isSplitable(job, path)) {  *//如果文件可分割*  *//block大小，默认128M* long blockSize = file.getBlockSize();  *//获取每个分片的大小，根据computeSplitSize逻辑，得到每个分片为blockSize，默认就是与blockSize大小一致* long splitSize = computeSplitSize(blockSize, minSize, maxSize);   *//将文件大小赋值给bytesRemaining变量* long bytesRemaining = length;  *//该方法按照split大小进行逻辑切分文件，并将split存入splits数组中*  *//如果一个文件大小没有超过splitSize的1.1倍数，默认不切分分片* while (((double) bytesRemaining)/splitSize > *SPLIT\_SLOP*) {  *//获取切片对应block的索引信息* int blkIndex = getBlockIndex(blkLocations, length-bytesRemaining);  *//将当前切片对应的block节点信息存入splits数组中* splits.add(makeSplit(path, length-bytesRemaining, splitSize,  blkLocations[blkIndex].getHosts(),  blkLocations[blkIndex].getCachedHosts()));  bytesRemaining -= splitSize;  }   *//如果最后文件切分小于block 1.1倍大小，就是一个block，将block信息放入到splits即可* if (bytesRemaining != 0) {  int blkIndex = getBlockIndex(blkLocations, length-bytesRemaining);  splits.add(makeSplit(path, length-bytesRemaining, bytesRemaining,  blkLocations[blkIndex].getHosts(),  blkLocations[blkIndex].getCachedHosts()));  }  } else { *// not splitable*  *//如果文件不可分割* if (*LOG*.isDebugEnabled()) {  *// Log only if the file is big enough to be splitted* if (length > Math.*min*(file.getBlockSize(), minSize)) {  *LOG*.debug("File is not splittable so no parallelization "  + "is possible: " + file.getPath());  }  }  splits.add(makeSplit(path, 0, length, blkLocations[0].getHosts(),  blkLocations[0].getCachedHosts()));  }  } else {   *//Create empty hosts array for zero length files* splits.add(makeSplit(path, 0, length, new String[0]));  }  }  *// Save the number of input files for metrics/loadgen* job.getConfiguration().setLong(*NUM\_INPUT\_FILES*, files.size());  sw.stop();  if (*LOG*.isDebugEnabled()) {  *LOG*.debug("Total # of splits generated by getSplits: " + splits.size()  + ", TimeTaken: " + sw.now(TimeUnit.*MILLISECONDS*));  }  return splits; } |

关于MapReduce读取数据切片的源码总结如下：

1. MapReduce中默认的输入格式化类是TextInputFormat
2. 每个split大小默认和blockSize大小一样，为128M
3. 如果要调整split的大小可以通过调节mapreduce.input.fileinputformat.split.minsize或者mapreduce.input.fileinputformat.split.maxsize参数，假设想要调节Split大小为100M，那么就设置mapreduce.input.fileinputformat.split.maxsize为100M即可，如果要调节Split为200M，那么就设置mapreduce.input.fileinputformat.split.minsize为200M即可。
4. 如果一个文件大小没有超过splitSize的1.1倍数，默认不切分split。
5. 每个split都会对应到block块信息，进而得到当前块所在的节点信息，方便Map Task分发到对应节点上执行。

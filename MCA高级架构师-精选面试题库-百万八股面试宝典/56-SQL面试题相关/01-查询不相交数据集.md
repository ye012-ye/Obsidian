使用SQL查询a，b表中不相交的数据集。

a表：

|  |  |
| --- | --- |
| **id** | **name** |
| 1 | zhangsan |
| 2 | lisi |
| 3 | wangwu |
| 4 | maliu |
| 5 | tianqi |

b表：

|  |  |
| --- | --- |
| **id** | **name** |
| 1 | zhangsan |
| 2 | lisi |
| 3 | wangwu |
| 6 | zhaoqi |
| 7 | gaoba |

数据a.txt数据：

|  |
| --- |
| 1 zhangsan  2 lisi  3 wangwu  4 maliu  5 tianqi |

数据b.txt数据：

|  |
| --- |
| 1 zhangsan  2 lisi  3 wangwu  6 zhaoqi  7 gaoba |

SQL建表语句:

|  |
| --- |
| create table a(id int ,name string) row format delimited fields terminated by '\t';  create table b(id int ,name string) row format delimited fields terminated by '\t';    set hive.exec.mode.local.auto=true; |

SQL:

|  |
| --- |
| 第一种方式：select a.id,a.name,b.id ,b.name from a full outer join b on a.id = b.id where a.id is null or b.id is null;  第二种方式：select b.id ,b.name from a full join b on a.id = b.id where a.id is null union select a.id,a.name from a full join b on a.id = b.id where b.id is null;    select a.id,a.name from a full join b on a.id = b.id where b.id is null union select b.id,b.name from b full join a on a.id = b.id where a.id is null; |

现在有以下一个数据表

|  |  |
| --- | --- |
| **字段名称** | **备注** |
| id | 唯一ID |
| date | 日期（分区字段） |
| reg\_time | 注册时间（时间戳） |
| leave\_time | 注销时间（时间戳） |

请用一个sql计算2020年1月-2020年2月期间每天注册用户次日留存率？

举个例子：0101注册用户20人，0101注册用户在0102注销用户10人，次日留存率=10/20;0102注册用户30人，0102注册用户在0103注销用户15人，次日留存率=15/30

数据举例：

|  |
| --- |
| 1 20200101 1577885949 1577972349  2 20200101 1577885949 1578184749  3 20200101 1577885949 1577972349  4 20200101 1577885949 1578184749  5 20200101 1577885949 1577972349  6 20200101 1577885949 1578011949  7 20200102 1577925549 1578011949  8 20200102 1577925549 1578357549  9 20200102 1577925549 1578011949  10 20200102 1577925549 1578357549  11 20200101 1577885949 1578357549 |

sql结果：

|  |
| --- |
| select `date` , cast(sum(if(datediff(from\_unixtime(cast (leave\_time as bigint) ,"yyyy-MM-dd"),from\_unixtime(cast (reg\_time as bigint) ,"yyyy-MM-dd"))>1,1,0))/count(\*) as decimal(5,2)) as rt from temp group by `date`;  或者  select `date`,from\_unixtime(cast(reg\_time as bigint),"yyyy-MM-dd") as reg\_time,cast(sum(if(datediff(from\_unixtime(cast(leave\_time as bigint  ),"yyyy-MM-dd"),from\_unixtime(cast(reg\_time as bigint),"yyyy-MM-dd")) >1 ,1,0))/count(\*) as decimal(10,2)) as save\_rt from temp where `date` between '20200101' and '20200229' group by `date`,from\_unixtime(cast(reg\_time as bigint),"yyyy-MM-dd") ;  或者  select a.`date`,cast ((a.save\_cnt/b.reg\_cnt) as decimal(10,2)) as rt from (select `date`,count(\*) as save\_cnt from temp where datediff(from\_unixtime(cast(leave\_time as bigint),"yyyy-MM-dd"),from\_unixtime(cast(reg\_time as bigint),"yyyy-MM-dd")) >1 group by `date`) a join (select `date`,from\_unixtime(cast(reg\_time as bigint),"yyyy-MM-dd") as reg\_time,count(\*) as reg\_cnt from temp group by `date`,from\_unixtime(cast(reg\_time as bigint),"yyyy-MM-dd")) b on a.`date` = b.`date` where b.`date` between '20200101' and '20200229' |
